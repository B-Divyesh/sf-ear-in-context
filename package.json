{
  "name": "ear-in-context",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:claims": "npm run build && node scripts/claims.mjs",
    "test:browser": "npm run build && node scripts/claims.mjs",
    "test:live": "node scripts/claims.mjs",
    "audit:a11y": "node scripts/a11y-audit.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "axe-core": "^4.10.3",
    "playwright": "1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
